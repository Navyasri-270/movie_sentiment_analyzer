services:
  - type: web
    name: movie-sentiment-analyzer
    runtime: python
    buildCommand: pip install -r requirements.txt && python notebooks/train_models.py
    startCommand: uvicorn backend.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
